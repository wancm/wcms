<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Depends on Domain and on nothing else. The absence of a reference to Infrastructure is the
    whole point of the layering, and ArchitectureTests asserts it.
  -->
  <ItemGroup>
    <ProjectReference Include="..\ContentImporter.Domain\ContentImporter.Domain.csproj" />
  </ItemGroup>

  <!-- The provider mappers are internal; the tests need to see them. -->
  <ItemGroup>
    <InternalsVisibleTo Include="ContentImporter.Tests" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="8.0.2" />
  </ItemGroup>

</Project>
